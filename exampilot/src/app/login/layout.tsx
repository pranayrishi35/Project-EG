import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign In — ExamPilot",
  description: "Sign in to ExamPilot with Google or a magic link to access your personalised study planner.",
};

export { default } from "./page";
